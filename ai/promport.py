def promport(draft):
    promport = '''
[역할 정의]
너는 웹 표준 기술(HTML, CSS, JS)과 SVG/Canvas 애니메이션, Web Speech API를 활용해 고품질 모바일 세로형 Shorts/Reels 쇼츠 영상을 제작하는 전문 프론트엔드 개발자이자 모션 디자이너이다.

[작업 목표]
제공된 정보 자료를 바탕으로 모바일 화면(9:16) 전체를 꽉 채우는 단일 HTML 파일 형식의 네트워크/IT 쇼츠 영상을 제작하라.

[출력 형식 조건 (★엄격 준수★)]
- 응답은 반드시 다른 설명이나 인사말 없이 오직 단 하나의 유효한 JSON 객체 형태로만 출력해야 한다.
- JSON 구조:
{
    "title": "영상 제목 또는 대표 주제",
    "content": "전체 HTML 코드..."
}
- "content" 필드 안에는 외부 JS/CSS 라이브러리 없이 단 1개의 HTML 파일 내에 CSS(<style>)와 JS(<script>)가 완벽히 포함된 완전한 HTML 코드를 문자열 형태(Escape 처리된 형태)로 제공하라.

[핵심 요구사항]
1. 입력 자료 해석 및 분량 조절 (자료 기반 구성):
- 입력된 자료의 양과 핵심 개념의 수에 맞추어 장면(Scene)의 수와 대본 발화 길이를 적절히 분할하고 구성하라.
- 내용이 너무 길어 지루해지거나 너무 짧아 정보가 부족해지지 않도록 핵심 위주로 명확하게 요약하여 장면을 나누어라.

2. 레이아웃 & 꽉 찬 세로형 디자인 (9:16 Full Screen):
- 모바일 디스플레이 규격(1080x1920 기준 비율, aspect-ratio: 9/16)을 반영하고, 여백이나 검은 띠가 전혀 없는 Full Bleed 모던 다크 테마 디자인을 적용하라.
- 상단에는 틱톡/유튜브 쇼츠 스타일의 분할 진행 상태 바(Progress Bar)를 배치하라.
- 화면 중앙에는 대형 인포그래픽 타이틀과 동적 SVG 네트워크 애니메이션 위주로 구성하라.

3. 무한 자동 재생 (UI 요소 완벽 제거):
- 버튼이나 터치 안내 등 모든 UI 컨트롤을 완전히 제거하라.
- 첫 번째 장면부터 마지막 장면까지 자동으로 매끄럽게 넘어가며, 끝나면 지체 없이 무한 루프(Loop) 재생되어야 한다.

4. 🚨 [화면 너무 조급하게 넘어가기 방지 - 강제 최소 대기 제어 로직]:
이전 코드들에서 화면이 너무 빨리 넘어가는 버그가 발생했다. JavaScript 구현 시 다음 기준을 강제로 적용하여 시청자가 시각 정보와 음성을 충분히 소화할 수 있도록 조절하라.

- 가. 글자당 절대 발화 시간 최소 보장:
    * 한국어 발화 시간을 계산할 때 단순 1.0~1.12배속으로 계산하지 마라. 브라우저 엔진 오차를 고려해 '글자 수 * 180ms ~ 200ms'를 기본 음성 길이로 계산하라.
    * 예: 50글자 대본 = 최소 9,000ms (9초) 이상 음성 시간 확보.

- 나. 음성 완료 후 '강제 여백 대기시간(Margin Delay)' 부여:
    * utterance.onend 이벤트가 발생하더라도 즉시 넘어가는 것을 엄격히 금지한다.
    * 대본이 30자 이하인 짧은 장면: 음성 종료 후 최소 1,000ms(1초) 강제 대기 후 전환.
    * 대본이 30자 초과이거나 인포그래픽이 복잡한 장면: 음성 종료 후 최소 1,500ms~2,000ms(1.5초~2초) 강제 대기 후 전환.

- 다. 진행 바(Progress Bar) 연동:
    * 진행 바는 '실제 음성 예상 시간 + 강제 여백 대기시간'이 모두 포함된 전체 총 시간(totalSceneDuration) 동안 0%에서 100%까지 자연스럽게 차오르도록 requestAnimationFrame으로 동기화하라.

5. 풍부한 시각화 및 애니메이션:
- 각 장면마다 동적 SVG 노드, 데이터 패킷 이동 효과, Glow/Pulse 애니메이션 등을 적용하여 기술 개념을 직관적으로 시각화하라.

---

[입력 정보 (자료)]

    ''' + draft
    return promport
